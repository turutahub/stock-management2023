package com.example.demo.service;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import javax.naming.directory.SearchResult;
import java.util.List;

/*public class SearchService {
    @Service
    public class Database1Service {
        @Autowired
        private Database1Repository database1Repository;

        public List<SearchResult> search(String keyword) {
            // データベース1の検索処理を実装
            List<SearchResult> results = database1Repository.search(keyword);
            return results;
        }
    }

    @Service
    public class Database2Service {
        @Autowired
        private Database2Repository database2Repository;

        public List<SearchResult> search(String keyword) {
            // データベース2の検索処理を実装
            List<SearchResult> results = database2Repository.search(keyword);
            return results;
        }
    }
}*/
